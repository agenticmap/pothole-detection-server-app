{
  "name": "pothole-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Operator dashboard for the pothole-detection server (Phase 2.5).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.2.1"
  }
}
